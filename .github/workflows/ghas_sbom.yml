name: Generate SBOM & VEX

on:
  push:
    branches:
      - main

jobs:
  sbom-vex:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install system tools
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl

      - name: Install Syft
        run: |
          curl -sSfL https://raw.githubusercontent.com/anchore/syft/main/install.sh \
            | sh -s -- -b /usr/local/bin

      - name: Install Vexy and bleeding‑edge OSV client
        run: |
          pip install --upgrade pip
          pip install vexy
          pip uninstall -y osv
          pip install git+https://github.com/google/osv-python.git@main

      - name: Generate CycloneDX SBOM
        run: syft . -o cyclonedx-json --file sbom.cdx.json

      - name: Patch metadata.tools for Vexy
        run: |
          jq '
            .metadata.tools = (
              .metadata.tools.components
              | map({
                  vendor: .author,
                  name:   .name,
                  version:.version
                })
            )
          ' sbom.cdx.json > sbom.cdx.patched.json

      - name: Create Vexy config
        run: |
          cat <<EOF > vexy.yml
          sources:
            osv:
          EOF

      - name: Generate VEX document
        run: |
          vexy \
            --config vexy.yml \
            --in-file sbom.cdx.patched.json \
            --format json \
            --output sbom.vex.json

      - name: Upload SBOM & VEX artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sbom-and-vex
          path: |
            sbom.cdx.patched.json
            sbom.vex.json
